# Nerrico frontend configuration
# Copy to `.env.local` to override. All values are optional.

# Backend API base URL (the Settings page value in the browser wins over this)
VITE_API_BASE_URL=http://localhost:4000
